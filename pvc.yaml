apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lh-app-pvc
  namespace: memos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
